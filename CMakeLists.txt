cmake_minimum_required(VERSION 2.8.12)
project(ddz)
set (CMAKE_CXX_STANDARD 14)

find_package(pybind11 REQUIRED)
pybind11_add_module(ddz core.h module.cpp)
if(MSVC)
  target_compile_options(ddz PRIVATE /utf-8)
endif()
# add_executable(main module.cpp)
